node_modules

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# amplify
# amplify_outputs.json est volontairement versionne : le build ne deploie
# pas encore le backend, le site publie doit donc pointer vers celui existant.
# Il ne contient que de la configuration client publique (ids de pool, URL
# AppSync, cle d API en lecture seule) — deja visible dans le bundle navigateur.
.amplify
amplifyconfiguration*

# divers
.DS_Store
.env*.local
.npm
data/product-images.generated.json

# Claude Code (reglages locaux, propres a chaque poste)
.claude/
